import { makeStyles } from '@material-ui/core';

const regularContentStyles = makeStyles(theme => ({
    btn: {
        color: '#F9F9F9',
        padding: '10px 60px',
        borderRadius: '40px',
        fontWeight: 600,
        textTransform: 'capitalize'
    }
}));

export default regularContentStyles;