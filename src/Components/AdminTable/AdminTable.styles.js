import { makeStyles } from '@material-ui/core';

const adminTableStyles = makeStyles((theme) => ({
  deleteIcon: {
    color: '#F05700',
    fontSize: 15,
    cursor: 'pointer'
  },
}));

export default adminTableStyles;
