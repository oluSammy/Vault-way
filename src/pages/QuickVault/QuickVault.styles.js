import { makeStyles } from '@material-ui/core';

const quickVaultStyles = makeStyles(theme => ({
  root: {
    minHeight: '85vh',
    padding: '40px 50px 70px 0'
  }
}));

export default quickVaultStyles;