import { makeStyles } from '@material-ui/core';

const fundMyVaultStyles = makeStyles(theme => ({
  root: {
    minHeight: '85vh',
    paddingTop: '50px',
    paddingRight: '40px'
  }
}));

export default fundMyVaultStyles;