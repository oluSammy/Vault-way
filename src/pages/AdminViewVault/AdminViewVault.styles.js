import { makeStyles } from '@material-ui/core';
const adminViewVaultStyles = makeStyles((theme) => ({
  interestTxt: {
    color: '#ADB8CD',
    fontSize: 14,
    fontWeight: 500,
    marginTop: -12,
    marginBottom: -5
  },
  interestNumber: {
    color: '#6E80A3',
    fontWeight: 500,
    fontSize: 18,
    marginTop: -12,
    marginBottom: -5
  },
}));

export default adminViewVaultStyles;
